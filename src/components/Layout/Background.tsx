import React from 'react';
import { createTheme } from '@material-ui/core';

export const theme = createTheme({
  palette: {
    background: {
      default: '#a2b9bc', // Set your desired background color here
    },
  },
});
